# Threaded data extraction class for bybit kline data

example usage can be found in main.py

Will need to have bybit API keys saved in this location
os.path.join(os.path.expanduser("~"),"0_cred","bybitAPI.txt")
